{
  "name": "starter-project",
  "version": "1.0.0",
  "description": "Starter template basic",
  "scripts": {
    "dev": "vite --port 3000",
    "build": "vite build",
    "preview": "vite preview --port 3001",
    "deploy": "surge ./dist"
  },
  "devDependencies": {
    "surge": "^0.23.1",
    "vite": "^5.0.11"
  },
  "dependencies": {
    "nanoid": "^5.0.4"
  }
}
